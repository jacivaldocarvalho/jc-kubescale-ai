apiVersion: v1
kind: Service
metadata:
  name: {{ include "jc-kubescale.fullname" . }}-api
  labels:
    {{- include "jc-kubescale.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "jc-kubescale.selectorLabels" . | nindent 4 }}