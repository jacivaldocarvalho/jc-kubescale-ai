apiVersion: v1
kind: Service
metadata:
  name: jc-kubescale-autoscaler
  namespace: jc-kubescale
  labels:
    app: jc-kubescale-autoscaler
spec:
  type: ClusterIP
  ports:
    - port: 8080
      targetPort: 8080
      name: http
  selector:
    app: jc-kubescale-autoscaler